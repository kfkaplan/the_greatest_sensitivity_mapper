name: the_greatest_sensitivity_mapper
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - ipython
  - numpy
  - scipy
  - matplotlib
  - astropy=4.1
  - numba
  - ipywidgets
  - pip
  - pip:
      -reproject
